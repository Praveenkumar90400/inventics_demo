apiVersion: v1
kind: Service
metadata:
  name: tiktak-service
spec:
  selector:
    app: tiktak
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer # Or NodePort, depending on your needs
